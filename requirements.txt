# Core — CPU-only PyTorch (Streamlit Cloud has no GPU)
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.6.0+cpu
torchvision==0.21.0+cpu

# Data / numerics — versions with Python 3.13 wheels (no source build)
numpy>=2.0.0
pandas>=2.2.3

# Image processing
opencv-python-headless>=4.10.0
Pillow>=10.4.0
albumentations==1.4.3

# ML utilities
scikit-learn>=1.5.0

# Visualization
matplotlib==3.8.4
seaborn==0.13.2

# App
streamlit>=1.40.0
plotly==5.22.0

# Utilities
tqdm==4.66.4
